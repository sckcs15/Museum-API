import "./css/reset.css";
// public 에서 생성한 css는 적용이 안된다?
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
 
    <App />
  
);
